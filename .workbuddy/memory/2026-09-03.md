# 2026-09-03（承接 09-02 深夜，跨零点）

## 线上白屏事故：根因钉死 = Streamlit Cloud 嵌入包装页把 app 压成 150px

### 部署拓扑（此前不知道，查出来才明白白屏为什么与 app 代码无关）
```
chilam.club          A 记录 54.215.31.113 → 301 → www.chilam.club
www.chilam.club      GitHub Pages（Server: GitHub.com），仓库 Chilamlam/chilam.club
                     内容仅两个文件：CNAME(15B) + index.html(406B)
index.html           <iframe src="https://chilam-club-app.streamlit.app/?embed=true">
?embed=true          ★不是 app 本体★，是 Streamlit Cloud 的「嵌入包装页」
                     包装页内部再套 <iframe src="//.../~/+/?embed=true"> 才是真 app
```
**app 代码与壳页在两个不同仓库**：业务在 `chilam-club-web`，壳页在 `chilam.club`。
以往只在 `chilam-club-web` 里排查，永远查不到白屏——**排障前必须先确认部署链路本身**。

### 实测数据（CDP 驱动无头 Chrome，穿透 OOPIF 读每层 frame）
| 被测对象 | 内层真 app iframe 高度 | 渲染结果 |
|---|---|---|
| 本站 `?embed=true`（线上现状） | **CSS高=150px** clientH=150 offsetH=150 rectH=150，父 `DIV._stateContainer_aycw8_26` height=150px | 只出 `Built with Streamlit 🎈 Fullscreen` 共 34 字 |
| 官方 `30days.streamlit.app/?embed=true` | **同样 150px**，父节点同样 `_stateContainer_*` 150px | 同样只出徽标 |
| 本站 `/~/+/?embed=true`（绕开包装页） | 铺满视口 900px | hasApp=True、可见文字 3411、markdown 192、图表 3 |

**官方示例 app 出现完全相同的 150px 约束 → 平台侧回归，与本站代码/配置无关。**
（参照物走独立通道，符合「参照不能与被测对象共享故障模式」铁律。）
Streamlit 状态页 `All Systems Operational`、无未解决事件 → **平台自己不认为这是故障，
等修复没有时间表**，只能自己绕。

### 反证：我推的代码没搞坏任何东西
直连 `/~/+/` 与 `/~/+/?embed=true` 均渲染完整（含「统计日 20260902」），
`page_fibonacci.py`/`page_live_quote.py` 那批改动无关。前轮 AppTest 结论一致。

## 修复方案与 A/B 反向验证（`shell_ab.py`）
把壳页 iframe 的 src 从 `…/?embed=true` 改为 **`…/~/+/?embed=true`**（跳过包装页）。
同一套浏览器、同一套判据，量测两份真实 index.html：

| 视口 | OLD（线上现状） | NEW（修复版） |
|---|---|---|
| desktop 1280×900 | **FAIL** 内层被钳高 clientH=150 | **PASS** 铺满 900px、md=192、图表 3、文字 3411 |
| mobile 390×844 | **FAIL** 同样 150px | **PASS** 铺满 844px、md=192、文字 3424 |
| 第三方 Cookie 限制开（模拟隐私模式，实测拦下 9~11 个 Cookie） | **FAIL** 仅 34 字 | **PASS** 900px、md=192 |

判据设计（防假断言）：
- **元断言**：必须真的出现跨站 iframe（OOPIF），否则报 NOTHING 而不是 PASS；
  Cookie 组必须真的观测到 `blockedCookies>0`，否则「限制未生效，本次什么都没验到」。
- **高度阈值**：app 高度须 ≥ 视口 80%，否则算「被钳制」——只判 hasApp 会被 150px 蒙过去。
- **A/B 双向**：OLD 若也 PASS 则判据抓不住白屏（假断言），结论作废；NEW 若不 PASS 禁止上线。
- **造错反验**（`fix_verify.py`）：
  - `Z_broken` 不存在的 host → 落到 `share.streamlit.io/errors/not_found`，0 字，FAIL ✔
  - `Z2_wrongsite` example.com → 真的产生 OOPIF 且有 129 字，但 hasApp=False，FAIL ✔
    （证明判据不会被「无关内容」喂饱）
- **判据放宽后复验**：为容纳官方旧版 app（无 `stMarkdown` testid）把判据放宽成
  `md>20 或 textLen>500`，放宽后重跑造错组仍全 FAIL，才敢用。
- **横向验证 `/~/+/` 是平台通用能力**：官方 `30days.streamlit.app/~/+/?embed=true`
  同样 PASS（1013 字）→ 不是本站私有实现，方案可上线。

**残留风险（已如实记录）**：`/~/+/` 是 Streamlit Cloud 内部路径、非公开 API，
平台若再改会二次失效。彻底免疫的做法是壳页 302 跳到 `chilam-club-app.streamlit.app`
（代价：地址栏不再是自有域名）。

回滚材料：原 index.html 已备份 `Temp/diag/shell_index_ORIGINAL_BACKUP.html`
406 字节、blob sha `ec12d7b8f60a6d5cfbc49976d5f974a6ac8ae9d5`；
新版 LF 后 2507 字节、blob sha `3add7b4ca978e85c7391dd3c2bbe12da170ef582`。

## 数据停更事故：根因是 cron 派发被丢弃，不是延迟（`sched_lag2.py`）
用户报障时刻 09-02 22:55 CST（14:55 UTC），当时**最后一次成功完成的定时跑批是
20.7 小时前的 #342（09-01 18:11 UTC）** → 用户看到 09-01 数据，如实反映当时状态。

诚实性修正：GitHub API 的 run 对象**不暴露是哪条 cron 触发的**，
第一版脚本按「最近计划时刻」硬归因，延迟数小时时会张冠李戴（把 11:37 延迟 4h
误判成 14:20 的班），得出「主班自 08-26 起失灵」这种不可靠结论。改成只报可证事实：
**计划 72 条 / 实际 40 条 → 至少丢弃 32 条（44%）**，这是不依赖归因的下界。

延迟分布（每日最早 run 相对 11:37 UTC）：
```
07-29 ~ 08-26   +0.2h ~ +2.3h   正常抖动
08-27           +9.5h   ← 拥塞骤然恶化
08-28          +12.0h
08-29 ~ 09-02   +3.6h ~ +6.5h   持续劣化，且多数日子当天只产生 1 条派发
```
即 `schedule` 是 best-effort，**高负载时整条派发被丢弃**（不是延后执行），
被丢时无邮件、不进状态页、Actions 列表无痕迹 → 表现为「数据停更且看不到失败记录」。
`tools_gh_watchdog.py` 的注释里已经写明这一点，判据是结果导向（今天 data/ 有没有今天数据），
**但它没有被任何东西定时调用**——这是本次事故真正的缺口。

远端 data/ 现状复核（`data_fresh.py`，读文件内日期字段而非 commit 时间）：
`strong_stocks/strong_etfs/breakout_stocks/market_sentiment/limit_ladder/
speculation_data/ai_market_analysis` 全部 = 2026-09-02，`index_history.csv` 尾行 09-02，全批一致。

### 新踩的坑：日期正则会把非日期数字认成日期
`data_fresh.py` 把 `index_history.csv` 判成「最新 2027-10-27」，实际文件里
最大日期是 09-02——`20271027` 是别的字段（疑似 ID）被 `(20\d{2})(\d{2})(\d{2})` 误匹配。
**扫全文取最大日期的做法必须限定列，否则新鲜度断言会被无关数字喂饱**（假 PASS 的典型）。
本次靠 `tail` 复核尾行才发现。

## 部署后线上复核（`live_verify.py`）：证明「线上此刻真的好了」

**这一步不能省，也不能用 A/B 结果代替。** `shell_ab.py` 量的是本地两份 index.html，
只能证明「新写法可行」；线上是否真的部署成功、CDN 是否已刷新，是另一件事。

壳页部署事实（带外复核，非本地缓存）：
- `Chilamlam/chilam.club` 的 `index.html` blob = `78ded55ad406`（1441 字节），
  提交 `f6434ae12d1d`「fix: 壳页 iframe 指向 app 本体 /~/+/」。
- 直接 `curl https://www.chilam.club/` 下载的内容与本地 `shell_index_NEW.html`
  **逐字节一致**（blob 同为 `78ded55ad406`）→ CDN 已刷新到新版。
- 注：记忆里此前写的「新版 2507 字节 / blob `3add7b4c`」指的是 `shell_new_lf.html`
  这个中间稿，**实际部署的是 1441 字节的 `shell_index_NEW.html`**，别记错回滚基线。

CDP 无头 Chrome 实测 `https://www.chilam.club/`（带 `?cb=时间戳` 绕缓存）：

| 用例 | 结果 | 证据 |
|---|---|---|
| 线上现状 desktop 1280×900 | **PASS** | app 铺满 900px、markdown 192、图表 3、可见文字 3485 |
| 线上现状 mobile 390×844 | **PASS** | app 铺满 844px、markdown 192、图表 3、可见文字 3498 |
| `Z_old` 负对照：现场把 iframe src 改回 `/?embed=true` | **FAIL** ✔ | 只出「Built with Streamlit 🎈 Fullscreen」34 字，内层 `/~/+/?embed=true` clientH=150 |

负对照的意义：**证明判据真能抓住这个故障**。若负对照也 PASS，说明判据是假断言、
本次「线上好了」的结论必须作废（脚本里 `exit 2` 直接判死）。造错生效性也有守卫：
改到 0 个 iframe 就报「造错未生效，本次什么都没验到」，不判 PASS。

顺带确认了 150px 钳制的确切位置：包装页里内层 iframe 的 `clientH=150 / CSS高=150px /
父CSS高=150px`，而新写法下 `clientH=900 / CSS高=900px / 父CSS高=900px` —— 高度约束
来自包装页自己的 DOM，不是 app 侧样式，所以改 app 代码永远修不好。

## 跑批与数据现状（09-03 08:3x 复核）

- 远端 `data/` 内的交易日字段：`strong_stocks` / `breakout_stocks` / `limit_ladder`
  全部 = **2026-09-02**，与最后一次跑批一致（run #346，09-02 18:06 UTC 完成）。
- **09-03 尚无新跑批**（当日主班 11:37 UTC 还没到），属正常，不是停更。
- **watchdog 定时调用缺口仍未修**：`.github/workflows/` 只有 `daily_update.yml`，
  两条 cron（`37 11`/`20 14`），**没有任何东西调用 `tools_gh_watchdog.py`**。
  这是 09-02 停更事故的真缺口，本轮未动（PAT 只有 repo scope、改 yml 需 workflow scope）。

## 断网中断现场：`symbol_resolve.py` 是**半成品**，别当已完成

断网发生在写这个模块的中途。**现状必须如实记：**

- 文件在（159 行、`py_compile` 通过），但**只写完了「不联网的纯函数」部分**：
  `bare()` / `prefixes_for()` / `market_of()` / `guess_kind()` / `parse_quote()`
  + 三张表（`PREFIX_PRIOR` / `MAJOR_SH_INDEX` / `KLINE_EP`）。
- **docstring 承诺的联网消歧部分还没写**：`rank_key()`（活跃度排序）、
  `resolve()` / `resolve_candidates()`（探三个前缀后取舍并标 ambiguous）、
  批量取报价的 `fetch_quotes()` —— `grep def` 全部为 0 个。
- **零调用方**：全仓 `grep symbol_resolve` 只有它自己，`page_watchlist.py:46`
  与 `daily_digest.py:107` 仍是各自的老 `_tx_code`。**没有配套探针**。

→ 所以四处实现的收口**并未完成**，只是搭了地基。下次续做的顺序：
补 `rank_key`+`resolve_candidates` → 写 `tools_probe_symbol_resolve.py`（撞号断言必须
覆盖 `000905→中证500`、`000300`、`000016→上证50` 三个已实测的错例）→ 才轮到改调用方。

中途实测新增的两条接口事实（已在模块 docstring 里，值得单独记）：
- `000300` 用错前缀 `sz000300` 时腾讯**根本不返回该行**（不是空串也不报错）
  → 探三个前缀与探一个前缀开销相同，可以放心并发探测。
- 沪市指数**未开盘时仍返回昨收价**，只是 `amount=0` 且 `high/low=0`
  → 「取到报价」≠「取对标的」，撞号消歧第一判据必须是「当日是否真的在交易」
  （`alive = amount>0 or (high>0 and low>0)`），不能用「报价是否为空」。

---

# 09-03 续做（断网恢复后，上面「半成品」一节已过期，以本节为准）

## 撞号收口：已完成并上线

`symbol_resolve.py` 补完联网消歧（325 行，纯 stdlib）：`fetch_quotes` 三出口
（`None` 失败 / `{}` 确实不存在 / `{...}` 命中）、`rank_key`、`resolve_candidates`、
`resolve`、`is_ambiguous`、`describe_alternatives`、`kline_url`、`xueqiu_url`。

**消歧判据定稿**：`rank_key = (0 if is_major_sh_index else 1, 0 if alive else 1, _prior_rank, -amount)`。
- 白名单宽基**优先于活跃度**：接口对指数 amount 归零的瞬间，纯活跃度排序会滑向同号个股。
- `is_major_sh_index` **只看代码白名单、不信 `kind`**（kind 由 PE 段推断，会抖动）。
- `ambiguous` 判据 = `len(cands) > 1`，**不掺 alive**：09:15 实测盘前全市场 alive=False，
  掺进去盘前一条撞号都不上报。

调用方接上：`daily_digest.fallback_pct_map`、`page_watchlist.fetch_quotes`
（`BENCH_BARE=000300` 强制 `sh000300`）、`_fetch_daily_kline` 走 `sr.kline_url`、
`_tx_code` 降级为退路并标注「撞号会猜错、勿新增调用」。表格加「市场」列，
页脚 `st.caption` 列出撞号的另一选项。

探针：`tools_probe_symbol_resolve.py` 正向 24/24 + 反向 8 条造错全抓；
`tools_probe_digest.py` 126 项、`tools_probe_watchlist.py` 全绿。
联网实测 `000905→sh000905` 中证500 7729.22、`000016→上证50` 2914.44、`000300→沪深300`。

横向 grep 确认**无第五处**：`daily_speculation.py` 的 `qt.gtimg.cn` 来自 tushare
`ts_code`（SH/SZ 已定，非裸码）、`page_macro_erp.py` 硬编码 `sh000001`/`sh512010`/`sh512480`
——均无用户裸码撞号风险。

## numpy 缺口：从「补一行」升级为机器化自检

`tech_analysis` / `scorecard` / `sentiment` / `page_macro_erp` **顶层** `import numpy as np`，
但两份 requirements 都没声明——此前不崩只是 pandas 与 streamlit 都 Requires numpy 顺带装上，
**借来的运气**。两清单显式加 `numpy>=1.26`，并新建 `tools_probe_requirements.py`：
- A 闭包通道：AST 从入口（`app`+`pages/*` / `run_daily`+`daily_*`）递归本地模块，
  区分「顶层裸 import」（缺包即 ImportError → FAIL）与「软 import」（try/函数内 → 只提示）。
- B 点名通道：`MUST_DECLARE` 硬断言 numpy 在两份清单里。
- `DIST_TO_IMPORT` 映射发行名与导入名（pyjwt→jwt、google-generativeai→google 等）。
- 正向 8/8、反向 6 条造错全抓。首次跑 FAIL 4/8 如实抓住了缺口，补完才转绿。

## 看门狗：定时缺口已闭环，且脚本本身有两个会「空派补跑」的真缺陷

先说结论：**看门狗现在挂在 WorkBuddy 自动化上，不再依赖改不了的 workflow yml。**
两条 recurring（周一至周五）：
- **20:40 CST 主班巡检** → 跑 `tools_gh_watchdog.py`（不带参，缺数据会真派发补跑）。
  排在主 cron 19:37 + 全量 18 分钟 + 余量之后。
- **23:10 CST 兜底班复核** → 跑 `tools_gh_watchdog.py --check`（只查不补）。
  排在兜底 cron 22:20 之后。这个点再派发没意义：12~18 分钟后就过零点，
  跑批会按「数据是否已发布」重锚定，很可能把昨天的数据再写一遍。

**为什么走自动化而不是 workflow**：PAT 只有 repo scope，`.github/workflows/*.yml`
四条路（git push / Contents API / Git Data API）全部实测被拒，只能人工网页粘贴。
自动化是不需要 workflow scope 的等价方案，且本机不需要常开也能跑（云端调度）。

### 修掉的缺陷 ①：取不到 ≠ 数据旧（拿测量工具的故障当被测对象的故障）

实测现场：`tools_gh_watchdog.py --check` 跑了 **4m13s**，四个产物全 `HTTP=0`，
报「今日数据缺失」；**同一时刻 curl 落盘四个全 200**，读数都是 20260902。
旧版把 `HTTP=0` 直接算进 `ok=False`，于是**通道一抖就在默认模式下派发一次
12~18 分钟的全量补跑**。这正是记忆里那条「测量工具自己会骗人」的同型复发。

改法：`check_freshness` 返回**三态而非布尔** —— `fresh` / `stale` / `blind`。
- 主通道换成 **curl 落盘再读**（`-o /dev/null` 在本机会假报 exit=23/size=0，绝不用它判成败），
  urllib 退为备用；`fetch_remote()` 双通道串联。
- 一个产物都没读出日期 → `blind`、**退出码 9**、**绝不派发**，输出明说「什么都没验到」。
- 取到了但解析失败（列改名）→ 算 `stale` 而非 `blind`（文件在、内容不对，是真问题）。
- 只验到一个且是今天的 → 仍是 `stale`，不能报 fresh（其余三个未验证）。

### 修掉的缺陷 ②：跑批前「今天没有今天的数据」是正常态

10:07 去查必然查不到当日数据，旧版报「缺失」。若挂成每小时一次，整个白天都在空派，
而收盘前派发的跑批因行情未发布只会把昨天数据再写一遍（见
`daily_breakout.get_latest_trade_date` 的锚定规则）——纯烧 Actions 额度。
加 `judge_after()` = **主 cron + RUNTIME_MIN + JUDGE_MARGIN_MIN 三常量联动**
（改 cron 不会漏改判定线），早于判定线直接 return 1 且**不发起远端检查**（元断言守着）。

### 修掉的缺陷 ③：派发成功 ≠ 已修复

旧版派发成功返回 0，于是「今天很太平」与「今天出过事、修复结果无人复核」共用一个信号。
2026-09-01 的 RPS 正是这种形状：派发成功、退出码 0，数据依旧停在前一天。
新增**退出码 4 =「已派发、结果未验证」**，并由 23:10 那班复核兜底。

退出码全表：0 就位 / 1 无需跑批 / 2 确实缺失或派发失败 / 3 凭据问题 / 4 已派发未验证 / 9 什么都没验到。

配套 `tools_probe_gh_watchdog.py`：正向 **47/47**（含真联网 + 带外 curl 交叉复核 + 404 不当数据），
反向 **9/9 造错全抓**。所有会走到派发分支的用例都把 `dispatch` 换成计数器并断言次数
——探针本身绝不触发真跑批。

## 本轮反向验证踩的三个新坑（已写进 skill）

1. **等价改动 = 造错没造**：把 `minutes=RUNTIME_MIN + JUDGE_MARGIN_MIN` 造成 `minutes=53`，
   而 18+35 恰好 53 → 行为逐位相同，任何断言都抓不住。改成 90 才有区分度。
2. **`expect` 必须在失败行里命中，不能在全量输出里命中**：`ck(cond,msg)` 无论成败都打印同一句
   msg → 「别的断言挂了」+「msg 出现在某条 PASS 行」会被算成 caught。
   过滤时先取 `[FAIL]` 行与末尾清单行。
3. **期望关键字要取失败分支才会打印的文案**：`try: f(); bad("必须抛错") except: ck(True,"带列名")`
   造错后走 `bad()`，关键字写「带列名」永远命中不了。

## 推送记录（均已带外复核）

- `a5684e8` fix(watchdog) 双通道+三态+时点守卫：git push 直接成功，blob sha 逐个 MATCH。
- `8b5e7c6`→远端 `ba698bc` 退出码 4 + 记忆：**git push 与 fetch 全部失败**
  （`github.com:443` 连不上，21s 超时 ×4 次，还出现过一次 `Authentication failed` 假象），
  改走 `tools_gh_put_via_gitdata.py --msg "$(git log -1 --format=%B 8b5e7c6)" <4 个文件>`。
  API commit 与本地 **tree 同 sha**（`b153362f`）→ 内容逐字节一致，本地 `ahead 1` 是正常现象，
  **别去强推「对齐」**。四文件 blob sha 全 MATCH。
- `eb77fda`→远端 `d4af0fb` 注释校正：同样 push 失败走 API，tree `2a0b9142` 双向一致。
- **网络分层事实（本轮实测）**：`github.com:443`（git 传输）不可达；
  `api.github.com` urllib 4/4 200；`raw.githubusercontent.com` urllib 10:35 时 3/3 200
  ——**三个域名各自独立，一个不通不代表另一个不通**，判「GitHub 挂了」前先分域测。
- 时钟带外核对：本机 epoch 与 GitHub 响应头 `Date` 只差 1s，时钟可信。
  **Git Bash 无 tzdata**（`TZ=Asia/Shanghai` 与 `TZ=Foo/Bar` 都返回 GMT）→
  判北京时间一律用 Python 的 `timezone(timedelta(hours=8))`，别信 shell 的 TZ。

## 仍未闭环

- **09-03 当日数据尚未产出**（21:00 口径模拟查询：四产物全 `20260902`）。
  这是正常的——主 cron 19:37 还没到。**首次真实验证要看今晚 20:40 那班巡检**，
  它才是「自动化 → 脚本 → 派发」整条链路的第一次真跑。
- 跑批 workflow **没有 `concurrency` 段**：20:40 派发的补跑若拖到 22:20，会与兜底 cron 并跑。
  提交步骤是 `git pull --rebase && git push` 三次重试，多半能自愈，但两个 run 同时写 `data/`
  仍有互相覆盖的可能。加 `concurrency` 需要改 yml（workflow scope，改不了）→ 记着这个风险。
  **09-03 12:xx 实证补充**：拉最近 30 条 run 逐对比较起止时间，**重叠 0 对** →
  账本不会被并发覆盖，暂不为此引入守卫（真发生了再说，别为想象中的问题改动不了的文件）。
- `/~/+/` 平台路径残留风险、白屏长效监控：仍待上线观察。

---

# 09-03 12:xx 续做：账本 PII 收口 + 密钥点名判据收口（已推送 `848dddd`→远端 `7c5ae04`）

## 最严重的发现：仓库是 public，账本原设计会公开会员名单

`push_ledger.json` 落在 `data/digest/`，**随 Actions 一起提交**，而仓库是公开的。
原设计把 WxPusher UID 与订阅邮箱**原文**当账本键：
- UID 是**投递凭据** —— 配上同在跑批环境里的 `app_token` 就能冒名给该用户推消息；
- 邮箱是 PII，且「出现在账本里」本身就等于「此人是付费会员」。

改成 HMAC-SHA256 摘要键（`_target_key`）：
- 密钥取首个已配 secret：`DIGEST_LEDGER_SALT` → `SUPABASE_KEY` → `WXPUSHER_APP_TOKEN`，**绝不落盘**。
- **一个都没有时放弃记账**（返回 `""` → `ledger_mark` 跳过并告警、`ledger_seen` 恒 None
  → 当天多推一次）。**刻意不退化成硬编码兜底盐**：盐写在公开仓库里就是公开的，
  「已脱敏」的假象比不脱敏更危险（第一版真写了 `chilam-club-fallback-salt`，是假脱敏）。
- **必须 HMAC，不能裸 sha256**：邮箱可枚举，裸 sha256 账本 = 「输入邮箱查是否会员」的公开查询接口。
- 群标记 `__wecom__` 保留原文（不含个人信息，人工看账本要读得懂）。
- 残余暴露面（已知并接受）：账本仍泄露「某天有多少目标收到推送」= 会员规模下限。
  要连这个也藏起来得把账本移出仓库（存 Supabase），暂不做。

**带外复核**：远端 `data/digest/` 只有 `history/ latest.json latest.md`，
`git log --all -- data/digest/push_ledger.json` 为空 → **账本从未被提交过，无历史泄露**。
`grep -rlE "邮箱|UID_|AT_" data/` 全仓零命中。**在第一次落盘前赶上了。**

## 新增断言：账本能不能**跨 run 活下来**（此前完全没验）

「代码想把账本写在哪」证不到「它真能活到下一班」。一旦被 `.gitignore` 挡掉或 yml 漏
`git add`，每次 run 都读到空账本 → 去重全程空转、天天重复推，而日志照样打印
「账本已更新」——最典型的静默失败。补三条：
- `git check-ignore` 判未被忽略（退出码 1 才对）；
- **元断言校准探法**：拿已知被忽略的 `data/chilam.db` 验 check-ignore 真会报 0，
  否则上一条恒过；
- yml 提交步骤须 `git add data/`（正则匹配代码行，注释不算）。
反向 5/5：精确 ignore 账本 / `data/digest/*.json` 连带被忽略 / yml 只 add 子目录 /
yml 删掉 git add / 校准路径不再被忽略（元断言须报「探法失效」）。

## `tools_probe_digest.py` 密钥点名判据的三个洞（一条断言同时有漏抓和假通过）

1. **漏抓形态**：原只抓 `_env("XXX")` 字面量，抓不到 `for k in ("A","B"): _env(k)`
   这种元组间接引用 → `DIGEST_LEDGER_SALT` 静默漏检，断言在但覆盖不到 = 没有。
2. **假通过**：原判 `name not in wf`（yml 全文）。而这个 yml 的**注释里正当写满了
   secret 名**（解释为何回退 toJSON 透传）→「注释里提过」也算点名，注释不会变成环境变量。
   改成严格匹配映射行 `NAME: ${{ secrets.NAME }}`，顺带能抓「映射到错误 secret」的手滑。
3. **过度报警**：`_ledger_salt()` 是回落链，链上有人点名就不会静默失效，一律报缺会
   训练人忽略自检输出（和漏抓一样有害）。→ AST 识别「元组遍历 + 短路返回」的回落链，
   链上**至少一个**成员被严格点名时豁免其余；**一个都没点名照旧全员报缺**（豁免有条件），
   且**另有直接读取处的名字永不豁免**。
4. 扫描文件集补 `wxpusher.py` —— `WXPUSHER_APP_TOKEN` 的真实读取处在那里，
   原来只扫 `daily_*.py` + `database.py`，漏点名它也抓不到。

结论：131 项全过，`DIGEST_LEDGER_SALT` 因回落链豁免而**不需要改 yml**（PAT 无 workflow scope）。

## 反向验证第八、九条铁律（本轮新踩）

- **⑧ 造错用的 fixture 必须真能走到被测分支**。第一版检测器自测写
  `any(_env(k) for k in (...))` 当「无短路」的反例，但**生成器表达式在 AST 里是
  `GeneratorExp`、根本进不了 `ast.For` 分支** → 不管检测器怎么改它都返回 `[]`，
  这条断言永不失败。改成真的 `for` 语句才验到 `_short`。
- **⑨ 复合条件的每个子条件都要有专属断言守卫**。`if _short and _reads:` 只有两条
  fixture 时，把 `_reads` 整个删掉（变成「任意字符串元组 + 短路都算密钥回落链」）
  两条依然全绿 —— 靠**对探针自己造错**（`rv_digest_detector.py`）才发现，补第三条
  fixture `_FIX_NOREAD` 后 3/3。**没有断言守着的条件等于随时会失效。**

## 本轮反向验证总账

| 层 | 条数 | 造错对象 |
|---|---|---|
| 账本逻辑 | 17/17 | `daily_digest.py`（探针内建 MUTATIONS） |
| 账本持久化 | 5/5 | `.gitignore` / `daily_update.yml` |
| 密钥点名 | 8/8 | `daily_update.yml` + `daily_digest.py` |
| 检测器自测层 | 3/3 | `tools_probe_digest.py`（探针自己） |

正向：push_ledger 87、digest 131、wxpusher 124、run_daily 71、gh_watchdog 49/49，全绿。
每层反验结束都跑一次「复原自检 exit=0」，确认改文件的实验没留残渣。

## 推送（带外复核）

`848dddd` 本地 commit → `git push` 与 `git fetch` 均 40s 超时（`github.com:443` 老问题）
→ 走 `tools_gh_put_via_gitdata.py --msg "$(git log -1 --format=%B 848dddd)" <4 文件>`，
远端新 head `7c5ae04385e7`。**四个文件 blob sha 与本地 `git hash-object` 逐个 MATCH**
（`d09d3c6b46bb` / `d69bb0a2c288` / `68303f4a2a3c` / `cb986545b46f`）。
本地 `origin/main` 引用仍停在 `d3b5f7c`（fetch 不通，属缓存滞后，**别强推「对齐」**）。
提交前扫过暂存区，无 `AT_*`/`UID_*`/`eyJ`/真实邮箱夹带。

## 顺带确认的假象

`git status` 一度显示 ` M .gitignore`，但 `git hash-object` 与 `HEAD:.gitignore`
**同 sha**、`git diff --exit-code` 退出 0 → 只是反验脚本重写文件后 index 的 stat 缓存过期。
**判「文件被改了」要看内容 sha，不能只看 status 的脏标记。**




## 凭据卫生：两个高危后门已止血（工作区，未推送）

**发现**：全仓凭据扫描（`tools_probe_credentials.py`，AST 而非正则）查出两条比账本 PII 严重得多的洞：
1. `auth.py` 硬编码 JWT 默认密钥 `chilam_club_secret_key_2026_invest_secure` —— public 仓库可见，
   而带外复核确认 **Streamlit Cloud Secrets 里填的就是这一串**（指纹 `e4af8b5b44d4` 逐字节相同）
   → 任何人可离线自签 `{"is_admin":true}` token 直接拿后台。
2. `database.py::verify_password` 的 bcrypt ImportError 兜底里有
   `if email=="chilam@admin.com" and password=="<已在 git 历史公开的旧口令>": return True` —— 明文口令后门，
   且口令字面量写进 public 仓库。带外 pbkdf2 复算确认 **线上 id=1 管理员真口令就是这个**。

**已改（工作区未提交）**：auth.py 改为「未配→进程级随机 / 配了已泄露常量→**拒用**并降级 + 告警」（只告警=洞照样开）；
database.py 删明文放行分支；dashboard.py 不再把管理员登录邮箱当对外联系邮箱（默认清空）；
PLAN 文档删明文初始凭据。

**探针自身的假绿（重要教训）**：第 2 段用 `find_module`/`load_module` 屏蔽 streamlit，
这套旧式 finder 协议 **Python 3.12 已移除** → 3.13 上完全不生效，子进程照读本机 secrets，
「未配密钥」分支从没被走到；而兜底元断言写成链式 `a != b != c` 恰好漏掉 `none vs leak` 那一对。
已改用 `sys.modules['streamlit'] = None`。**凡是「屏蔽某依赖」的测试，都要有「屏蔽是否真生效」的元断言。**

**反验（tools_probe_credentials_reverse.py）**：16 项有效 / 6 项无效。6 项两处根因：
- M4 三条 `.streamlit/secrets.toml…` 断言前 22 字完全相同 → `find()` 固定宽度截断命中 3 条（**反验脚本 bug**）。
- M1 预期推演错：第二层防御（拒用逻辑）挡住了 M1 造的洞，伪造 token 那两条本就不该翻；
  另断言消息里嵌了动态值 `来源=…`，造错后文案变化导致归一化键漂移。
- **根治方向：给每条断言配稳定 ID，反验按 ID 比对，不再拿消息文本当键**（消息里嵌动态值必然漂移）。

**剩余唯一 FAIL**：线上管理员口令仍是那个已公开的旧口令（M7 反向造错证明该 FAIL 来自实时查询不是写死）——
代码清理只是止血，**口令进过 public 仓库只能靠轮换**，需用户决策。

## 口令轮换已执行 + 反验收口（34 有效 / 0 无效）

**轮换**（用户指定新值，走系统 temp 脚本，跑完即删，明文不落仓库）：
写前先验「旧口令能过」确认改的是那条记录 → PATCH `users.id=1` → 独立重读复算：
新口令能过=True、旧口令能过=False、哈希已变。探针随之 38 PASS / 0 FAIL（exit=0）。
**写生产库的三段式：前置校验确认目标 → 写入 → 独立重读复核**，缺最后一段等于没验。

**反验从 16 有效/6 无效 修到 34 有效/0 无效**，两处根因：
1. **M4 是反验脚本自己的 bug**：按消息前 22 字定位，而三条 `.streamlit/secrets.toml…`
   断言前 22 字完全相同 → 命中 3 条。**根治：探针每条断言加稳定 ID（A01..A38），
   反验按 ID 比对**，与消息文案彻底解耦（消息里嵌 `来源=…`、`长度=…` 必然漂移）。
2. **M1 是我预期推演错了，断言本身没毛病**：M1 单独造错只翻 A11/A16 ——
   因为**第二层（拒用已泄露常量）把洞接住了**。补 M1b（M1+M2 两层一起拆）后
   一次打掉 9 条（A11/A16/A17/A18/A19/A20/A21/A25/A27）。
   **「造错后仍 PASS」不等于假断言 —— 纵深防御下，一层被打穿不该翻的断言就是不该翻。**

**顺手修掉的真隐患**：探针汇总处 `sys.exit(1 if FAIL else 0)` —— 上面刚声明
「未验证不是通过」，转头就给未验证发绿灯。CI 里没有 secrets.toml，接进去会永远绿着
却一条都没查。改成 `sys.exit(1 if (FAIL or UNVERIFIED) else 0)`，并加 M8 守住。

**新增造错**：M1b（两层同拆）、M7 反向（口令轮换后 A38 变恒 PASS，改为喂必然命中的值
逼它翻 FAIL；口令走环境变量 `REVERSE_CURRENT_PW`，不落仓库）、M8（切断带外通道，
验「没查到」被记为未验证且退出码非 0）。

**工具坑**：给探针批量插断言 ID 时，用正则匹配 `ck(` 会把 ID 插进 docstring 里
「每个 ck() 必须用唯一 ID」这句话，直接改坏文件 → **改代码里的代码必须用 AST**。
