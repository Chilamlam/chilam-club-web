# 2026-09-01 工作记录

## 补完 8/31 未收尾的两件事（409 归因修复 + 推送）

### 再补一处：`[]` 被当成绑定成功

`bind_wxpusher_uid` 首版只判 `res is None`。但 PostgREST 在
`Prefer: return=representation` 下，PATCH **零行命中返回 `[]`**，
而 `[] is not None` 为真 → 账号不存在也会报「绑定成功」。
这正是自己写在 MEMORY 里的那条「`None` 与 `False`/`[]` 必须分开」，
修 409 归因时只顾着区分错误码，又把空结果当成了成功。

补 `isinstance(res, list) and len(res) == 0` 分支，文案指向「账号可能已不存在，
请重新登录」。自检同步加一条，反向验证（删掉该分支）准确报 1 项失败。

**模式又出现一次：修一个「失败没说清」的 bug 时，顺手造了个「失败说成成功」的。**
改返回值语义时，三个出口（None / 空 / 有值）要一次列全。

### 顺手发现的孪生：`update_user_watchlist` 也一样

同一个 `res is not None` 在自选股保存里有一份。抹掉它，两处共用一条规则。
另外修掉一条**永不失败的断言**：digest 探针原先在全文件里裸匹配
`return res is not None` 来校验 `update_user_watchlist`，而同文件另一处
（`update_user_wxpusher_uid`）也有这行 —— 把 watchlist 函数整个删掉它照样「通过」。
改为先切出函数段再断言。反向验证：删除整个函数 → 报 3 项；只去 `[]` 判定 → 报 1 项。

### 自检汇总（改后复跑）

| 探针 | 结果 |
|---|---|
| `tools_probe_wxpusher` | 98 项，0 失败 |
| `tools_probe_run_daily` | 68 项，0 失败 |
| `tools_probe_digest` | 122 项，0 失败 |
| `tools_probe_watchlist` | 全部通过 |
| `tools_check_st_api` | 无 Streamlit 不兼容调用（1.62.0） |

## 又一类「静默污染」：API 推送把 CRLF 写进了远端

核验推送结果时发现远端 `database.py` 比本地长 601 字符，而文件正好 601 行 ——
行尾差了一整轮。查下来：本机 `core.autocrlf=true` 且仓库**没有 `.gitattributes`**，
git 存进 blob 的是 LF、工作区检出的是 CRLF；而 `tools_gh_put_via_gitdata.py`
绕过 git 直接写 blob，把工作区的 CRLF 原样传了上去。

后果不是「风格不统一」：下次 fetch 后这些文件会被 git 判成**整份已修改**
（工作区文件归一化成 LF 后 ≠ 索引里那份 CRLF 的 blob），之后任何一次提交
都会把整个文件的行尾翻一遍，diff 全是噪点，真正的改动反而看不见，
而且这种 diff 会反复出现、越滚越大。

处置：
- 脚本加 `_to_lf()`：上传前 CRLF→LF（含 NUL 的按二进制原样传，不误伤图片）。
- 拉仓库 tarball 全量扫了一遍行尾（比逐个 blob 请求快得多：115 个文件一个请求搞定），
  历史上被该脚本写成 CRLF 的共 8 个文件，一并重写；复检 115 个文件 CRLF 归零。

**教训：绕过 git 写仓库时，凡是 git 自己在提交路径上做的规范化，
绕过者都必须自己补上。** autocrlf 是最容易漏的一个，因为它平时完全隐形。

## 顺藤摸出的第三、四处：确认收款路径

改完绑定后顺手扫了剩下两处 `res is not None`，发现支付链路更严重：

1. `confirm_payment` 的 REST 兜底把订单置为 completed 的 PATCH **完全不看返回值**，
   失败照样 `return {"ok": True}` → 订阅已续期、订单仍 pending，
   管理员下次打开还看到这笔，再点一次就**重复续期**（收一笔钱发两次货）。
   改法不是笼统返回失败：那样管理员第一反应就是重试，而部分成功时重试正是最糟操作。
   文案必须写「订阅已创建（到期 X），订单仍是 pending，**不要重复确认**」。
2. `cancel_payment` 零行命中返回 `[]` 被当成成功（订单不存在也报已取消）。
3. 两处前端取消**完全不看返回值**；`admin.py` 的确认失败提示紧跟 `st.rerun()`，
   会被瞬间吃掉——管理员只看到「点了没反应」。改走 `session_state` flash。

自检 98 → 104 项。**又踩了一次「永不失败的断言」**：flash 两条首版写成
`'admin_flash' in src`，而 `pop("admin_flash")` 那行也含这个字符串，
把两处赋值删掉照样通过。改成校验赋值形式 `st.session_state["admin_flash"] =` 后才拦得住。
反向验证：删写入 → 准确报 2 项。

## 环境坑：`git update-ref` 写 refs/remotes 静默失效

对照实验（仓库只有 .sample 钩子）：写 `refs/heads/zz_probe` 成功、
写 `refs/remotes/origin/main` 返回 0 但引用没变。更糟的是 `fetch` 会把
远端跟踪引用**整个清空**（packed-refs 只剩表头、`refs/remotes/origin/` 目录消失），
只留 `FETCH_HEAD`。所以对齐流程改成：fetch 拿到对象 → `git reset --mixed <已知远端 sha>`
→ 手动 `mkdir -p .git/refs/remotes/origin` 并写文件补回引用。
**每次 fetch 后都要复核 `git for-each-ref refs/remotes`。**

## 夜间跑批自己跑成了

9/1 04:29 CST 有 `Auto-update daily data [skip ci]`（`c5506d5`），
说明 cron 这次没被平台丢弃——与 8/31 那次「彻底不派发」对照，
正好印证 best-effort 的随机性：同一份配置，有时准点、有时丢。
看门狗仍需保留。

## 待办（用户明天自行验证）

顺序不能乱：注册新账号 → 下单看管理员微信是否收到告警 → 管理员进
`pages/admin.py` 点确认收款 → **最后**才测绑定。
绑定必须用**第二个微信**：管理员微信是下单告警的唯一收件人，
为测绑定而解绑它会同时打瞎告警通道。

## 板块轮动功能上线（同日新增）

需求：近 10 个交易日涨幅 Top10 题材概念 + 主升/轮动判读。

### 数据源定调（重要，勿再试错）
- **东财 clist 概念板块（fs=m:90+t:3）一次请求组拿全量 ~504 个**，窗口涨幅直接给：
  **f109=5日 / f160=10日 / f110=20日 / f24=60日**（用东财自家板块日K逐一交叉验证，
  误差 ≤0.06pp；**f110 是 20 日、f160 才是 10 日，与直觉相反**，勿「纠回」）。
  pz 上限 100 → 6 页翻完；请求字段自选（akshare 的 name_em 只映射了部分字段）。
- akshare 的 THS 历史函数真名是 `stock_board_concept_index_ths`（无 period 参数），
  同名概念板块与东财成分不同，量级差 1.5~7pp——**跨源对账只能验数量级不能验精确值**。
- push2his 板块日K用 curl 直连可用（akshare 走它被 RemoteDisconnected），
  但**本机高频探测 push2/clist 半小时就触发 IP 级限流**（实测 urllib+curl 全灭 ~10 分钟），
  每日一次的量没事，本地调试别连打。
- 同花顺题材无免费全量日频源（name_ths 无涨跌幅、历史逐板块限流）→ 用东财概念口径，
  前端如实披露。

### 实现要点
- 三层：`sector_rotation.py`（计算层，纯 pandas）/ `daily_sector_rotation.py`
  （跑批：翻页拉快照→归档 history.csv→算 analysis.json）/ `page_sector_rotation.py`+app.py 路由。
- 判读=Top10 与上一交易日重合度：≥7 主升 / ≤3 轮动 / 4~6 过渡，理由带原始数字。
- 属性型板块黑名单（昨日*/重仓/股通/MSCI…）子串匹配，刻意保守。
- 跑批守卫：周末不落盘；「快照与归档最后交易日涨跌幅一致率 ≥98%」判非交易日/停滞；
  **守卫只在归档还没有今天时生效**（同日重跑是幂等替换，第一版把重跑也拦了）。
- completeness 对**服务端 total** 判（rows < total*0.95 → incomplete），别用硬编码阈值
  ——首跑 TLS 吃了第 5 页（400/504），硬编码 400 会误判 complete。
- 单页失败不放弃后续页（TLS 断连只咬某几页），连续 2 页失败才停。

### 探针教训
- 夹具新旧板块涨幅区间必须完全隔开（30 段 vs 15 段），否则并列值排序漂移
  让「重合数」断言失败得莫名其妙——这不是生产 bug，是夹具语义错了。
- 断言「理由带重合数字」要匹配 `3/10` 这种核心片段，别把文案前缀（「重合仅」）写死进断言。

### git 通道死亡时的本地/远端对齐（本次实测）
- API(gitdata) 推送成功后本地 git 通道 fetch 全灭 → 远端 commit 对象本地拿不到。
- **正确对齐姿势**：比对两端 commit 的 **tree sha**（相同=内容逐字节一致，密码学保证）；
  本地 main 保留自己的 commit（同树同父），远端跟踪引用**不要**手写指向缺失对象
  （`for-each-ref` 会 fatal 129），退回上一个有效 sha 即可。
- 下次 fetch 成功后 `git pull --rebase` 自动收敛：同树同父 → 本地补丁判为已应用 →
  干快进到远端 sha。**无需也绝不能强推**。
- **别走 commit-tree 重建 sha 的弯路**：API /git/commits 不支持 raw Accept，
  message 尾字节/时区差一位 sha 就不同，两次重建全失败（纯浪费时间）。
  远端状态判断永远只认 API /commits/main + blob sha 抽查（git hash-object 对比）。
- 本次最终状态：远端 main=be9852a（tree 6d597a5d），本地 a24db33（同 tree），
  sector_rotation.py 远端 blob=本地 51606aab ✓。

## 排名矩阵（10×10）上线（同日追加）
- 需求：行=名次 1~10（1/2/3 加粗），列=交易日（最新在最左），格=该日该名次板块 + 当日涨跌幅。
- 排名口径与页面主榜一致（当日 10 日涨幅），格内展示当日涨跌幅——既能查某日排名，也能跨日看名次进退。
- 计算层 `build_rank_matrix()`（JSON 可序列化，页面 st.cache_data(ttl=600) 包一层现算，不占 analysis.json）。
- 渲染走 `ui_compat.html_embed`（st.iframe data-URI，跨版本安全），名字深色+涨跌幅红涨绿跌两行格；
  归档不足 10 天时如实显示「已积累 X/10 个交易日」，缺失格 —，不拿别日顶替。
- 归档现状：仅 1 天（昨晚本地试跑写入的 2026-09-01 行，盘中快照）——今晚 Actions 19:37 收盘跑批
  同日幂等替换为真实收盘值，之后每晚 +1 列自然补齐。未做历史回填（clist 无历史，逐板块 K 线 504 请求必限流）。
- 探针加 test_rank_matrix（夹具三段涨幅区间隔开防并列漂移）+ 接线断言（rank<=3 hot 类、
  html_embed、缺失格 —、[-days:][::-1] 倒序），反向验证 5 项真实失败后恢复全绿。

## 整站实用价值评估（用户提问，结论待落地）
- **定位定论**：本系统是「情绪与结构仪表盘」，不是选股器。事实层（情绪四期/涨停连板/
  板块矩阵/突破池）可直接依赖；推断层（龙头雷达/黄金分割/技术形态）需自行验证。
- **重大发现：RPS 强度排序已被自检证伪** —— data/scorecard/performance.json 显示
  T+1/T+5/T+10 超额收益中位数**全为负**、方向胜率 <50%，站内自己写「排序信息量存疑」。
  → 任何涉及 RPS 排序的前端文案都应加免责，勿包装成选股依据（**待办，未改**）。
- 付费建议（给用户，未执行）：**现在不要做付费订阅**。卖点是「纪律闭环」而非「预测准确」；
  若要设门槛，只锁「我的池子/战绩回看/作业本/推送」（个人数据与投递），
  所有行情与判断类页面永久免费 —— 合规风险最低（不荐股不给价不给仓位）。
  建议先免费积累 2~3 个月真实战绩样本再谈定价。
- 当前实况：情绪四期=退潮期（晋级率 15.6%、溢价中位数 2.33%）；板块矩阵仅 1 列，
  需 10 个交易日成型；情绪归档最新为 20260831（正常，9/1 数据要等当晚 19:37 跑批）。

## 付费墙现状盘点（用户提问，仅盘点未改动）
实际门禁只有 3 处（8/29 调整后，全市场客观数据一律开放）：
1. `app.py:714` 「我的池子」→ 只判 `is_logged_in()`（**登录墙，非付费墙**，会员≠必需）
2. `page_digest.py:80` 摘要页「推送订阅」→ `is_vip()`（内容免费、**只锁投递**）
3. `database.py:get_push_recipients()` 投递名单 → 有效订阅 + 管理员豁免 + digest_optin
   （唯一真正消耗付费权益的执行点）
辅助：`page_watchlist.py:534` 用 is_vip 决定绑定入口是否展开（提示强度差异，非门禁）。
**发现文案 bug（待办，未改）**：`pages/dashboard.py:132` 仍写「升级 VIP 可解锁强势股 RPS 动量/
投机与套利/投资作业本/自选股雷达」——这 4 项在路由里**早已全部开放**，属于对未开通用户的
虚假承诺，会导致付费后发现「本来就能看」。应改为只承诺推送投递 + 个性化段落。

## 付费墙文案与实际门禁对齐（已落地）
- **改动 1** `pages/dashboard.py:131` 免费用户提示：删掉「升级 VIP 可解锁强势股 RPS 动量/
  投机与套利/投资作业本/自选股雷达」（这 4 项 8/29 起早已全开），改为如实写
  「所有行情与判断页面免费开放；VIP 唯一权益=收盘摘要推送到微信+你的池子今日段落」。
- **改动 2** 下单按钮正上方新增 st.info「开通后得到什么 / 不包含什么（不会解锁任何新页面）」
  + 合规 caption（不推荐个股、不给价位仓位）。付费前最后一屏必须如实交代。
- **改动 3** `app.py` `render_vip_lock` → **改名 `render_login_lock`**：它唯一调用点
  「我的池子」只判 `is_logged_in()`，却写着「VIP 会员专享模块」并给「前往会员中心开通」按钮
  → 把只需免费注册的用户推去付费。新文案明说「免费注册即可完整使用，无需开通 VIP」。
- **改动 4** `tools_probe_wxpusher.py` 新增 `check_paywall_claims()`（113 项断言）：
  从 app.py **路由段推导**真实 VIP 门禁数（`auth.is_vip()` 出现次数必须为 0），
  再校验会员中心「解锁」承诺不落在免费页面集合上 + 必含「不会解锁任何新页面」
  + 登录卡片不得写 VIP 专享。**门禁与文案从此互为断言**，改一边忘另一边会自己红。
- 探针写法坑：文案断言必须先 `_strip_comments()` 剥掉 `#` 注释、并跳过函数 docstring
  —— 否则「解释历史错误文案」的注释本身命中断言，逼人删掉最该留的教训（实测误报 2 项）。
- 反向验证：改回旧承诺 → 2 项失败；给战绩回看加 `and auth.is_vip()` → 门禁推导断言失败；
  恢复后 113 项全绿。既有探针无回归（quote_api/tech_analysis/watchlist 的失败均为
  本机联网受限，与本次改动无关）。
