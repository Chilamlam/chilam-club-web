# 2026-08-29 工作日志

## 修复投资作业本页面 TypeError 崩页（commit 91bb61a）

**现象**：Streamlit Cloud 上「投资作业本」整页崩溃，traceback 指向
`app.py:512  st.image(info['avatar'], use_column_width=True)` → TypeError。

**根因**：Streamlit 新版本已彻底移除 `st.image` 的 `use_column_width` 参数
（历史演进：`use_column_width` → `use_container_width`(deprecated, 2025-12-31 移除)
→ `width="stretch"`）。`requirements.txt` 里 streamlit 未锁版本，Streamlit Cloud
自动升级到 1.5x/1.6x 后旧参数直接报错，导致整页白屏。

**修复**：
- 新增 `ui_compat.py` → `image_stretch(image, **kwargs)`：按
  `width="stretch"` → `use_container_width=True` → `use_column_width=True` → 默认
  顺序 try/except TypeError 降级，任何 Streamlit 版本都不崩。
- `app.py` 两处大佬头像（512/529 行）、`pages/dashboard.py` 四处收款码
  （微信/支付宝各 2 处）全部改用 `image_stretch`。
- `requirements.txt`：`streamlit` → `streamlit>=1.49,<2.0`。
- 新增 `tools_check_st_api.py`：AST 遍历全仓库所有 `st.xxx(...)` 调用，
  用 `inspect.signature` 比对关键字参数在当前装的 Streamlit 版本是否还存在。
  用法：`python tools_check_st_api.py .`。实测 1.62.0 下全仓库 0 处不兼容。

**验证方式**（可复用）：
1. 建独立 venv 装目标版本：`python -m venv .../envs/stcheck` +
   `pip install -i https://pypi.tuna.tsinghua.edu.cn/simple streamlit==1.62.0`
   （官方源在本机会 ConnectionResetError 10054，必须走清华镜像）。
2. 跑 `tools_check_st_api.py .` 做静态扫描。
3. 写临时冒烟页只渲染出问题的组件，`streamlit run _smoke.py --server.headless true
   --server.port 8899`，curl 取 HTTP 200 且日志 0 条 traceback，然后删掉临时页。

**顺带同步**：`git fetch` 后再次遇到 `.git/refs/remotes/origin/main` 静默不更新
（packed-refs 停在旧值），照旧 `printf <sha> > .git/refs/remotes/origin/main` +
`git merge --ff-only`，快进到 Actions 的 `00d0001`（13 个 data 文件，含 7 个
`data/gurus/*_latest.csv`，全部 pandas 可读）。之后提交并 push 成功，
远端 head = `91bb61a`（已用 GitHub API 核验）。

---

## 行情看板多市场真正接入（page_live_quote.py 重写，已推送 6e8f74d）

**用户诉求**：页面写着支持港股/美股/大宗商品且能选 5~60 分钟 K，实际全是空图。
要求「能接的真接入，接不了的就不要在页面上显示」。

**最终数据通道（穷举实测结论，改动前必须先跑 `tools_probe_quote_api.py` 复验）**

| 市场 | 报价 | 分时 | 分钟K(5/15/30/60) | 日/周/月K |
|---|---|---|---|---|
| A股/ETF/指数 | 腾讯 `qt.gtimg.cn/q=` | 腾讯 `minute/query` | 腾讯 `kline/mkline` | 腾讯 `fqkline` |
| 港股(含 HSI/HSCEI/HSTECH) | 腾讯 `hk00700` | 腾讯 332 点 | ❌ 无公开源 | 腾讯 `hkfqkline` |
| 美股个股 | 腾讯 `usNVDA` | 新浪 `US_MinlineService` 391 点 | 新浪 `US_MinKService` | 腾讯 `usfqkline`，**代码必须带 `.OQ`/`.N` 后缀**否则只 1~2 根 |
| 美股指数 | 腾讯 `usIXIC` | 新浪 `US_MinlineService?symbol=.IXIC`（**带前导点**） | 新浪 `US_MinKService?symbol=.IXIC` 1023 根 | 腾讯 `usIXIC`（**不带后缀**） |
| 国际商品 | 新浪 `hf_GC` | 新浪 `getGlobalFuturesMinLine` 1379 点 | ❌ 无公开源 | 新浪日K + `resample("W"/"ME")` 本地聚合 |

**放弃东财 `push2`/`push2his`**：`secid` 体系（`116.00700`/`105.NVDA`/`101.GC00Y`/`100.HSI`）
理论上全市场通吃且带中文搜索，但高频探测把本机 IP 打进限流后所有域名持续
`RemoteDisconnected` 超过 10 分钟，换 UA/Referer/数字前缀/`push2delay` 均无效。
**铁律：当场不可验证的接口不能作为线上依赖。**

**修掉的三个坑（自检从 11 失败 → 0 失败）**
1. `_tx_kline` 原用 `p.startswith("m")` 判断分钟线，**`"month"` 也以 m 开头**，
   月 K 被误发到 `mkline` 接口 → A股/港股/美股月K 恒为 0 根。改 `m5/m15/m30/m60` 白名单。
   （另实测 `fqkline` 的 `limit` 最大约 300，240 是安全值，与月K失败无关。）
2. `HSI` 同时在 `FUTURES_SYMBOLS`（恒指期货）和 `_HK_INDEX`（恒生指数），商品分支在前
   → 预设按钮「恒生指数」实际展示恒指期货。新增 `_FUTURES_AMBIGUOUS =
   {HSI,C,S,W,CT,CAD}`（这些与股票 ticker 撞名），裸输入按股票解析，
   要商品必须显式写 `HF_` 前缀（`HF_HSI`）。
3. 美股指数 `sina_sym=None` → 分时与 4 个分钟周期全空。新浪要 `.IXIC`、腾讯要 `usIXIC`，
   新增 `is_index` 标记在 `get_kline` 里分流。

**新增 `tools_probe_quote_api.py`（行情通道健康自检，无需 Streamlit runtime）**
用 `types.ModuleType` stub 掉 streamlit 顶掉 `st.cache_data`，直接调数据层，
对 9 个标的 × 各市场声明周期逐一验证，末尾打印失败数并以退出码返回。
必须用装了 pandas 的 venv 跑：
`/c/Users/Lenovo/.workbuddy/binaries/python/envs/stcheck/Scripts/python.exe tools_probe_quote_api.py`
（托管的 3.13.12 主解释器没装 pandas/plotly，直接跑会 ModuleNotFoundError。）

**架构约定**：`MARKET_PERIODS` 声明各市场真正可用的周期，页面 `st.radio` 由它动态生成；
缺失周期用 `MARKET_LIMIT_NOTE` + `st.caption` 说明原因。
**新增市场/周期时必须同步改这两个字典并跑自检，禁止让页面出现「能选但没数据」。**

**Git 侧**：`.git/refs` 下 `remotes/` 与 `tags/` 子目录再次整体消失（只剩 `heads/`），
导致 `git fetch` 成功但 `rev-parse origin/main` 停在 8-24 的 `306ef03`。
修复顺序：`mkdir -p .git/refs/remotes/origin` → 从 `.git/FETCH_HEAD` 第一列取 sha →
`printf <sha> > .git/refs/remotes/origin/main`。另外本次 GitHub 连接极不稳定，
`fetch`/`push` 需重试循环（第 4 次才通），已用 GitHub API 核验远端 head = `6e8f74d`
且 `page_live_quote.py` 624 行含 `_FUTURES_AMBIGUOUS`/`is_index`。

### 判断「Streamlit Cloud 是否真的部署了新代码」的版本指纹法
用户反馈「改完刷新还是不行」时，先不要怀疑代码，用 UI 文案做指纹比对：
本次新版把预设按钮改成 `纳指ETF` / `纽约黄金`，旧版是 `纳斯达克ETF` / `现货黄金`；
输入框 help 旧版是「支持 600519, 000001, 513100, 00700, AAPL, NVDA, GC 等」，
新版是「A股/ETF/指数: 600519…」。截图里出现旧文案 → 云端跑的还是旧代码，
与数据层无关。核验远端源码用 GitHub Contents API 取 base64 再 `count()` 关键字，
不要只看 commit sha。
处置：`share.streamlit.io` → 该 app 右侧 `⋮` → `Reboot app`（webhook 偶尔不触发，
自动部署不保证）。

### 港股分时崩页 + 曲线错位（已推送 bd95b00）

**崩页 `TypeError: float() argument must be ... not 'NAType'`**
位置 `_render_minute_chart` 均价线：`df["vol"].cumsum().replace(0, pd.NA)` 之后
`.astype(float)`。**Streamlit Cloud 的 pandas 已是 3.x，`replace(0, pd.NA)` 会把
float64 列退化成 object dtype**，除法结果也是 object，`astype(float)` 遇 `NAType` 崩。
本地 stcheck venv 是 pandas 3.0.5，用最小样例（前两分钟 vol=0）可稳定复现。
**铁律：需要「把 0 变成缺失值」时用 `s.where(s > 0)`，绝不要 `replace(0, pd.NA)`；
且 astype 一律写 `astype("float64")` 而非 `astype(float)`。**
另加 `pd.to_numeric(..., errors="coerce")` 兜底 + 空表走 `st.info`。

**曲线只画一截（错位）**
分时源会返回不在标准交易时段内的时间戳，merge 到 category 轴后全成 NaN：
- A股 267 点里 25 点落在 `15:06~15:30`（收盘后集合竞价/延时快照）
- 港股恒指最后一点 `18:31`（期指延伸报价）
- 港股收市竞价 CAS 落在 16:00 之后（腾讯给 `16:08`），原轴拉到 16:10 → 右侧空白+孤点
修法：新增 `_align_to_timeline(df, timeline)`，越界点吸附到「不晚于它的最后一个轴
刻度」（早于开盘的吸附到首刻度），同刻度 `drop_duplicates(keep="last")`；跨零点
市场（商品）先把小于开盘分钟数的时间 +1440 展平再比较。港股轴收到 `16:00`。
**校验口径：`_align_to_timeline` 输出的 time 必须 100% 在 timeline 内**，
9 个标的实测全 True，56 条渲染路径 0 异常。

---

## K线自动技术分析：缠论 + 帝纳波利（已推送 5d689e5）

**用户诉求**：行情本身没有壁垒（各家 app 都能看），要靠「自动结构识别」做差异化。
AskUserQuestion 确认范围 = 缠论 + 帝纳波利（不做波浪理论）、呈现 = 图上叠加 + 下方
结论文字、适用 = 全市场全周期。

**分层架构（务必保持）**
- `tech_analysis.py` 纯 pandas/numpy 计算层，**不 import streamlit**，可直接单测。
  这是本轮能快速定位线段/中枢 bug 的唯一原因。
- `tech_overlay.py` 纯 plotly/streamlit 绘制层：`draw_chan` / `draw_dinapoli` /
  `draw_macd` / `render_conclusion`，`ANALYSIS_MODES` 是 UI 选项唯一来源。
- `tools_probe_tech_analysis.py` 自检：9 标的 × 全 K 线周期 × 4 种图层组合。
  同样要用 stcheck venv 跑（主解释器没 pandas/plotly）。

**缠论实现口径（与经典定义有差异，docstring 与 UI 免责声明都已写明）**
K线包含处理 → 分型 → 笔 → 线段 → 中枢。笔严格口径合并K跨度 ≥4，笔数 < 5 时自动
降级到 ≥3。线段是「对笔端点再做分型」的工程近似，不是特征序列严格实现；中枢用
区间重叠法，不是「次级别走势类型」定义。缠论无唯一标准实现，隐瞒口径会让用户
误以为是权威结论。

**四个必须记住的实现陷阱（改动时不要退回原写法）**
1. **线段必须与「同型」邻居比较**。笔端点天然顶底交替，任何顶必然高于左右两个底，
   所以「比左右邻居更极端」的过滤恒成立 → 线段数恒等于笔数（曾出现 笔21段21、
   笔27段27）。改成顶比顶、底比底后：笔21→段8、笔27→段9。
2. **中枢 ZG/ZD 由前三段一次确定后固定不变**，延伸时只判 `min(h,zg) > max(l,zd)`
   是否重叠。绝不要在延伸时 `zg=min(zg,h)`/`zd=max(zd,l)` 收窄——那算出来是所有段的
   公共交集，不是中枢（茅台日K曾得宽 0.03% 跨 16 段 8 个月）。另加 0.3% 宽度下限
   过滤退化中枢 + `_PIVOT_MAX_LEGS=9` 封顶（缠论原意 9 段以上应升级级别，否则周K
   会出现跨 4 年的 18 段中枢）。
3. **价格格式化禁用 `:.4g`**（纳指会显示成 `2.591e+04`）。统一走 `_n()` 按量级分档：
   ≥1000 千分位整数、≥100 两位、≥1 三位、其余四位。
4. **ABC 目标位必须校验 C 落在 A~B 之间**，否则是同向延伸不是回撤（茅台周K曾出现
   C 高于 A、恒指算出 XOP 66.279）。不满足则回退一个端点重试，仍不行就明确输出
   「尚未形成有效的 ABC 回撤结构」；C 已被后续走势突破时用 `confirmed=False` 标注。

**帝纳波利部分**：DMA 3x3/7x5/25x5（右移意味着尾部悬空预测段，plotly category 轴下
用 `T+1`/`T+2` 虚拟标签接续）、Fibnode F3=0.382 / F5=0.618、COP/OP/XOP =
C + 0.618/1.0/1.618 ×(B−A)、Confluence 汇聚区（不同摆动 Fibnode 价差 < 1.5% 合并）、
MACD 8/17/9 与顶底背离。叠加帝纳波利时自动隐藏 MA5/MA20，否则图面糊成一片。

**页面接线**：`_render_kline_chart` 返回 `analyze()` 结果供文字结论复用，避免重复计算；
分时分支不做结构分析并用 caption 说明原因；`MIN_BARS=30`，n<30 直接拒绝。

**验证**：自检失败项 0；边界 n=5/15/29 正确拒绝、n=30 起出结构；全平盘/含 NaN/含 0
三种脏数据不崩；188 条渲染路径 0 异常；`tools_check_st_api.py` 在 1.62.0 无不兼容参数；
真实 Streamlit 冒烟 HTTP 200、traceback 0。行情通道自检仍 0 失败。

**本次版本指纹**：侧边栏菜单文案「⚡ 实时行情 + 技术分析」（旧版「⚡ 实时行情 (分时/K线)」）。
Git 侧本次 `.git/refs` 完好、fetch/push 各一次成功，远端 head 已用 API 核验 = `5d689e5`。

---

## 全球核心资产看板的硬编码假数据（page_macro_erp.py Tab3，严重问题）

**用户反馈**：截图显示纳指 100 = 19,845.2、标普 = 5,620.8、黄金 = $2,512.4、原油 = $75.8、
A50 = 12,180、USDCNH = 7.1420，全是 2024 年的价格水平。

**根因**：`page_macro_erp.py` 第 853 行 `macro_items = [...]` 是**一整块写死的字符串字面量**，
从来没有接过任何行情接口。这不是「数据没更新」，而是页面从上线起就在展示假数据。
与 8-28 的 `limit_ladder.json` 占位数据属同一类错误——**看板数值必须来自实时接口，
取不到就显示「—」，绝不允许硬编码兜底**。

**真实价格对照（2026-08-29 实测）**：纳指100 = 29,433（假数据低了 33%）、
标普 = 7,711（假数据低了 27%）、黄金 = $4,454/oz（假数据低了 44%）、
WTI = $83.47（假数据低了 9%）、A50 = 14,662、USDCNH = 6.7302（人民币实际比假数据强 6%）。
**用假数据做宏观择时会得出完全相反的结论**，比数据停更严重得多。

**修复：`GLOBAL_ASSETS` 声明式配置 + 两个批量取数函数**
配置元组 = `(key, 源, 源代码, 展示名, 价格格式, 类型标签, 解读)`
- `_fetch_tx_assets(codes)` —— 腾讯批量，一次请求取多个：`qt.gtimg.cn/q=usNDX,usINX,usDJI,hkHSI`
  `~` 分隔字段：`[3]`现价 `[4]`昨收 `[31]`涨跌 `[32]`涨跌% `[30]`时间 `[1]`名称
- `_fetch_sina_assets(hf, fx)` —— 新浪批量，**`hf_`(国际商品) 与 `fx_`(外汇) 可以混在同一次
  `list=` 请求里**（实测可行，省一次往返），但两类字段语义完全不同：
  - `hf_XXX`：`,` 分隔，`[0]`现价 `[7]`昨收 `[13]`名称 `[6]`时间 `[12]`日期，**无现成涨跌幅须自算**
  - `fx_XXX`：`[8]`现价 `[10]`涨跌% `[11]`涨跌 `[9]`名称 `[0]`时间 `[17]`日期
- `@st.cache_data(ttl=60)` + 页面「🔄 刷新报价」按钮走 `get_global_assets.clear()` + `st.rerun()`
- 单项失败不影响其余，缺失显示「—」并 `st.warning` 说明「不会用历史值冒充实时价」

**代码选择实测结论（不要再试错）**

| 资产 | 正确代码 | 踩过的坑 |
|---|---|---|
| 纳斯达克 100 | 腾讯 `usNDX` | `usIXIC` 是纳斯达克**综指**(26,402)，不是 100(29,433)，差 3000 点 |
| 标普 500 | 腾讯 `usINX` | `usSPX` 返回空 |
| 道琼斯 | 腾讯 `usDJI` | — |
| 恒生指数 | 腾讯 `hkHSI` | — |
| A50 期货 | 新浪 `hf_CHA50CFD` | `hf_CN` 空 |
| 现货黄金 | 新浪 `hf_XAU`（伦敦金）| `hf_GC` 是纽约期金，差 ~50 美元；要「现货」必须用 XAU |
| WTI 原油 | 新浪 `hf_CL` | — |
| 伦铜 | 新浪 `hf_CAD` | `hf_HG` 是美铜（单位不同，663 vs 14278），`hf_ZSD` 是伦锌 |
| 离岸人民币 | 新浪 `fx_susdcnh` | 裸 `USDCNH` 空 |
| 美元指数 | ❌ 无源 | `hf_DX` / `hf_DINIW` 全空 |
| 美债 10Y | ❌ 无源 | `usUS10Y` / `usTNX` / `hf_US10Y` 全空 |

**昨收字段已用日K交叉核验**：`hf_XAU[7]=4601.58` 与日K 2026-08-27 收盘 4601.580 完全一致，
`hf_CL[7]=83.53` 与日K 08-27 收盘 83.550 一致 → `[7]` 确为昨收，自算涨跌幅口径正确。

**顺带新增内容**：道琼斯、恒生指数、伦铜三张卡（原来只有 6 张），以及**铜金比**
（LME 铜 / 现货黄金 = 3.206）——工业需求 vs 避险需求的经典比价，只看方向不看绝对值。

**新增自检 `tools_probe_macro_assets.py`（5 条口径）**：price 正有限、`|pct| ≤ 30%`、
时间戳在最近 5 天内（**直接拦截「假数据/过期数据」这类问题**）、格式化无科学计数法、
源码无硬编码价格残留。须用 stcheck venv 跑。实测**失败项 0**。
坑：时间戳格式三家不统一——腾讯美股 `2026-08-28 17:15:59`、腾讯港股 `2026/08/28 18:31:31`、
新浪 `2026-08-29 04:59:58`，正则必须写 `[-/]` 兼容两种分隔符。

**全仓库硬编码扫描已做**：除本次修掉的一处，其余 `*_items`/`*_data` 均为空列表初始化或
菜单文案，无第二处假数据。
