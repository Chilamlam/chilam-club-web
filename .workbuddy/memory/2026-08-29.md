# 2026-08-29 工作日志

## 修复投资作业本页面 TypeError 崩页（commit 91bb61a）

**现象**：Streamlit Cloud 上「投资作业本」整页崩溃，traceback 指向
`app.py:512  st.image(info['avatar'], use_column_width=True)` → TypeError。

**根因**：Streamlit 新版本已彻底移除 `st.image` 的 `use_column_width` 参数
（历史演进：`use_column_width` → `use_container_width`(deprecated, 2025-12-31 移除)
→ `width="stretch"`）。`requirements.txt` 里 streamlit 未锁版本，Streamlit Cloud
自动升级到 1.5x/1.6x 后旧参数直接报错，导致整页白屏。

**修复**：
- 新增 `ui_compat.py` → `image_stretch(image, **kwargs)`：按
  `width="stretch"` → `use_container_width=True` → `use_column_width=True` → 默认
  顺序 try/except TypeError 降级，任何 Streamlit 版本都不崩。
- `app.py` 两处大佬头像（512/529 行）、`pages/dashboard.py` 四处收款码
  （微信/支付宝各 2 处）全部改用 `image_stretch`。
- `requirements.txt`：`streamlit` → `streamlit>=1.49,<2.0`。
- 新增 `tools_check_st_api.py`：AST 遍历全仓库所有 `st.xxx(...)` 调用，
  用 `inspect.signature` 比对关键字参数在当前装的 Streamlit 版本是否还存在。
  用法：`python tools_check_st_api.py .`。实测 1.62.0 下全仓库 0 处不兼容。

**验证方式**（可复用）：
1. 建独立 venv 装目标版本：`python -m venv .../envs/stcheck` +
   `pip install -i https://pypi.tuna.tsinghua.edu.cn/simple streamlit==1.62.0`
   （官方源在本机会 ConnectionResetError 10054，必须走清华镜像）。
2. 跑 `tools_check_st_api.py .` 做静态扫描。
3. 写临时冒烟页只渲染出问题的组件，`streamlit run _smoke.py --server.headless true
   --server.port 8899`，curl 取 HTTP 200 且日志 0 条 traceback，然后删掉临时页。

**顺带同步**：`git fetch` 后再次遇到 `.git/refs/remotes/origin/main` 静默不更新
（packed-refs 停在旧值），照旧 `printf <sha> > .git/refs/remotes/origin/main` +
`git merge --ff-only`，快进到 Actions 的 `00d0001`（13 个 data 文件，含 7 个
`data/gurus/*_latest.csv`，全部 pandas 可读）。之后提交并 push 成功，
远端 head = `91bb61a`（已用 GitHub API 核验）。

---

## 行情看板多市场真正接入（page_live_quote.py 重写，已推送 6e8f74d）

**用户诉求**：页面写着支持港股/美股/大宗商品且能选 5~60 分钟 K，实际全是空图。
要求「能接的真接入，接不了的就不要在页面上显示」。

**最终数据通道（穷举实测结论，改动前必须先跑 `tools_probe_quote_api.py` 复验）**

| 市场 | 报价 | 分时 | 分钟K(5/15/30/60) | 日/周/月K |
|---|---|---|---|---|
| A股/ETF/指数 | 腾讯 `qt.gtimg.cn/q=` | 腾讯 `minute/query` | 腾讯 `kline/mkline` | 腾讯 `fqkline` |
| 港股(含 HSI/HSCEI/HSTECH) | 腾讯 `hk00700` | 腾讯 332 点 | ❌ 无公开源 | 腾讯 `hkfqkline` |
| 美股个股 | 腾讯 `usNVDA` | 新浪 `US_MinlineService` 391 点 | 新浪 `US_MinKService` | 腾讯 `usfqkline`，**代码必须带 `.OQ`/`.N` 后缀**否则只 1~2 根 |
| 美股指数 | 腾讯 `usIXIC` | 新浪 `US_MinlineService?symbol=.IXIC`（**带前导点**） | 新浪 `US_MinKService?symbol=.IXIC` 1023 根 | 腾讯 `usIXIC`（**不带后缀**） |
| 国际商品 | 新浪 `hf_GC` | 新浪 `getGlobalFuturesMinLine` 1379 点 | ❌ 无公开源 | 新浪日K + `resample("W"/"ME")` 本地聚合 |

**放弃东财 `push2`/`push2his`**：`secid` 体系（`116.00700`/`105.NVDA`/`101.GC00Y`/`100.HSI`）
理论上全市场通吃且带中文搜索，但高频探测把本机 IP 打进限流后所有域名持续
`RemoteDisconnected` 超过 10 分钟，换 UA/Referer/数字前缀/`push2delay` 均无效。
**铁律：当场不可验证的接口不能作为线上依赖。**

**修掉的三个坑（自检从 11 失败 → 0 失败）**
1. `_tx_kline` 原用 `p.startswith("m")` 判断分钟线，**`"month"` 也以 m 开头**，
   月 K 被误发到 `mkline` 接口 → A股/港股/美股月K 恒为 0 根。改 `m5/m15/m30/m60` 白名单。
   （另实测 `fqkline` 的 `limit` 最大约 300，240 是安全值，与月K失败无关。）
2. `HSI` 同时在 `FUTURES_SYMBOLS`（恒指期货）和 `_HK_INDEX`（恒生指数），商品分支在前
   → 预设按钮「恒生指数」实际展示恒指期货。新增 `_FUTURES_AMBIGUOUS =
   {HSI,C,S,W,CT,CAD}`（这些与股票 ticker 撞名），裸输入按股票解析，
   要商品必须显式写 `HF_` 前缀（`HF_HSI`）。
3. 美股指数 `sina_sym=None` → 分时与 4 个分钟周期全空。新浪要 `.IXIC`、腾讯要 `usIXIC`，
   新增 `is_index` 标记在 `get_kline` 里分流。

**新增 `tools_probe_quote_api.py`（行情通道健康自检，无需 Streamlit runtime）**
用 `types.ModuleType` stub 掉 streamlit 顶掉 `st.cache_data`，直接调数据层，
对 9 个标的 × 各市场声明周期逐一验证，末尾打印失败数并以退出码返回。
必须用装了 pandas 的 venv 跑：
`/c/Users/Lenovo/.workbuddy/binaries/python/envs/stcheck/Scripts/python.exe tools_probe_quote_api.py`
（托管的 3.13.12 主解释器没装 pandas/plotly，直接跑会 ModuleNotFoundError。）

**架构约定**：`MARKET_PERIODS` 声明各市场真正可用的周期，页面 `st.radio` 由它动态生成；
缺失周期用 `MARKET_LIMIT_NOTE` + `st.caption` 说明原因。
**新增市场/周期时必须同步改这两个字典并跑自检，禁止让页面出现「能选但没数据」。**

**Git 侧**：`.git/refs` 下 `remotes/` 与 `tags/` 子目录再次整体消失（只剩 `heads/`），
导致 `git fetch` 成功但 `rev-parse origin/main` 停在 8-24 的 `306ef03`。
修复顺序：`mkdir -p .git/refs/remotes/origin` → 从 `.git/FETCH_HEAD` 第一列取 sha →
`printf <sha> > .git/refs/remotes/origin/main`。另外本次 GitHub 连接极不稳定，
`fetch`/`push` 需重试循环（第 4 次才通），已用 GitHub API 核验远端 head = `6e8f74d`
且 `page_live_quote.py` 624 行含 `_FUTURES_AMBIGUOUS`/`is_index`。
