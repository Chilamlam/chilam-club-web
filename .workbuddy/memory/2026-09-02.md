# 2026-09-02

## 行情页沪深撞号修复 + 名称搜索 + 北交所（已推送 `915011f`）

### 起因
用户查「中国稀土」出来「500低贝」。**两处协同失效**：
1. `resolve_symbol` 把所有 `000xxx` 硬编码成沪市（`s.startswith((...,"000","999"))`）；
2. `get_quote` 的另一前缀兜底判据是 `if not q` —— 但沪市指数**未开盘也返回昨收价**
   （`amount=0`、`high/low=0.00`、时间戳停在 09:00:00），有返回 → 兜底永不触发 → 静默出错。

> 教训：兜底条件写「有没有返回」是不够的，必须写「返回的是不是用户要的」。
> 注释里已预见歧义仍然出错，说明**判据选错比没有判据更危险**。

### 关键实测结论（勿再试错）
- 腾讯 `qt.gtimg.cn/q=sh000831,sz000831,bj000831` 一次请求返回多行；不存在的代码
  **整行不出现**（NO-LINE）→ 探三个前缀与探一个网络开销基本相同。
- **请求键必须原样小写**：`usAAPL` 返回 `v_pv_none_match`（不是空行），大小写敏感。
- 活跃度判据：`amount > 0 or (high > 0 and low > 0)`。`sz000806`（银河退，已退市）
  正确判 False。
- **但活跃度不够用**：实测 31 个 `000xxx` 样本里 **21 个沪深两市都在交易**
  （`000001` 上证指数 vs 平安银行、`000063` 上证周期 vs 中兴通讯）。故又叠一层
  `_MAJOR_SH_INDEX` 宽基白名单先验 —— 沪市 000xxx 段全是指数，但会被裸代码主动
  查询的只有上证指数/沪深300/上证50/中证500 这一小撮；其余（500低贝、上证周期、
  全R价值…）用户输入裸 6 位数字的意图必然是同号深市个股。
  `000002` 特意**不**列入（用户输的 000002 几乎总是万科Ａ）。
- **类型判定不能用段位启发式**：`p47/p48`（52周高低）恒为 -1 判指数、`p39`(PE) 空判基金，
  这套在「立讯转债」上错分为基金 → 改成**代码段确定性判定**（110/111/113/118/123/127/128
  = 转债）。且非 `sh/sz/bj` 前缀直接返回空串 —— 港美股同位段语义不同（`hkHSI`/`usIXIC`
  会被误判成基金），宁可不标类型也不给错的。

### 北交所（BJ_SHARE，此前完全无法查询）
- `bj` 前缀行情可用：`bj920002` 万达轴承有成交。
- **日/周/月 K 必须走 `newfqkline` 端点**；老 `fqkline` 会**静默只返回 1 根**
  （不报错、不为空、无任何异常）—— 这是本次最阴的坑。
- `mkline` 对北交所返回 `m5` 键但列表为空 → 该市场**不暴露分钟周期**。
- smartbox 搜不到北交所标的，也搜不到转债 —— 搜索层的已知局限。

### 名称搜索（第三层）
- `smartbox.gtimg.cn/s3/?t=all&q=<UTF-8 urlencode>`，需 UA + `Referer: stockapp.finance.qq.com`。
- **响应是 GBK，但查询词必须 UTF-8 编码**。本次踩坑：一开始把请求参数也按 GBK 编，
  导致中文关键词全挂而拼音/数字正常 —— 「中文挂、英文通」的现象要先怀疑参数编码，
  不要先怀疑限流。
- 返回 `前缀~代码~名称~拼音~类型码`，`^` 分隔；名称是 `\uXXXX` 需 `unicode_escape` 二次解码。
- 有过两次瞬时空返回 → 已加一次重试 + 降级（搜索挂掉不影响纯代码查询）。

### 自检体系（本次新增两个探针）
- `tools_probe_quote_api.py` 扩到 **82 项断言**：撞号 9 例 + 显式前缀 5 例 +
  北交所全周期 + 名称搜索；**加了根数下限断言**专防「端点选错只回 1 根」。
- 新增 `tools_probe_live_quote_page.py`：`streamlit.testing.v1.AppTest` **无头渲染页面**，
  验证选择器真出现、搜索按钮真生成。这个能力此前项目里没用过，值得推广到其他页面。
- 新增 `tools_probe_quote_reverse.py`：**5 种造错反向验证**，退出码 0/3。

### 反向验证抓出的两个真实假断言（最有价值的产出）
1. **失败出口不统一 = 断言在外部隐形**：B 段自己 `print("FAIL ...")` 没走 `bad()`，
   反向脚本按 `[FAIL]` 过滤时完全抓不到「北交所日K 只有 1 根」。造错生效了、
   自检也确实退化了，但外部看全绿。→ **探针里所有失败必须走 bad() 唯一出口**。
2. **断言被无关数据喂饱**：页面探针原判「按钮标签含中国稀土」，而 `PRESETS` 里本就有
   「中国稀土 `sz000831`」快速直达按钮 → 搜索挂掉照样通过。改判换行分隔符区分
   搜索结果按钮与预设按钮。
3. 另一个教训：**造错本身也要验证有效**。第一次造错用「URL 加个 `NOPE=1` 参数」，
   smartbox 直接忽略未知参数照样返回 —— 当时误判成假断言。改用「换不存在的 host」才真破坏。

### Git 传输（本次又遇到）
- `git fetch/push` 连续 `CONNECT tunnel failed, response 502`，但 API `curl` 200。
  → 走 `tools_gh_put_via_gitdata.py --msg "..." <多文件>` 单 commit 推送成功。
  page_live_quote.py 归一化掉 1088 处 CRLF（脚本内置 `_to_lf()`）。
- 推完后 fetch 恰好通了 → `git reset --mixed <远端sha>` 对齐。
- **`.git/refs/remotes/origin/main` 又不存在**（本机老问题）：`git reset` 成功但
  `git rev-parse origin/main` 报 `unknown revision`。手写该文件后 `for-each-ref` 复核通过。
- reset 后本地 `data/` 显示 28 个文件改动，实为**本地落后**（本地 derived.json 是
  20260831，远端已是 20260901）→ `git checkout -- data/` 用远端覆盖。
  **data/ 归 Actions 云端所有，本地永不 commit。**

### 终态
四探针全绿（82 项 + 4 项页面 + 5 项反向 + 技术分析），工作区干净，本地=远端 `915011f`。

---

## 任务二：RPS「反向超额」质疑 → 定位为口径缺陷（非算法 bug），推送 `8724570`

用户质疑：`performance.json` 显示 RPS 榜 T+1/5/10 超额中位数全负、方向胜率<50%，
「一般来说 rps 不会产生反向的超额」，要求先查算法、确认无问题后再加免责声明。
顺序不能颠倒——真有 bug 就该修 bug，不能用免责掩盖。

### 六轮只读诊断：数据层与计算层均无 bug（结论可复用）
排查顺序「数据完整性 → 计算复现 → 独立源交叉 → 成分体检 → 换基准 → 显著性」，
每轮都在仓库内 `_diag_tmp/`（**别用 `/tmp`，Windows 解析成 `C:\tmp`，这次又踩了**）：
1. 归档 42 交易日无缺口；日间跳变越界仅 2 例，均北交所（限 30%，合规）；
   隐含复权因子 175 只中仅 2 只 spread>2%。
2. 独立复现 `compute_returns` 与产出**完全一致**（T+5 median -0.0361 / dir 0.3864），
   排除「统计脚本与产出不一致」。收益分解：`ret_5` 中位 **-4.18%** vs `bench_5` **+0.05%**
   → 负 alpha 来自标的自身下跌，不是基准算反。最差样本逐日价格连续单调下跌
   （菲沃泰 5 日 -47.4%，43.5→42.7→37.9→34.2→27.4→22.9），是真实走势。
3. 腾讯前复权日K 交叉验证 4 只标的，**逐日数值完全一致，区间涨跌差 0.00%**。
4. 成分：232 只标的中 `688`+`301`+`300` 占 1341/2460（54%），典型小盘成长动量组合；
   ST 仅 14 条不构成主因；上榜标的**事前 50 日涨幅 +94%~+308%、250 日 +146%~+369%**。
5. 换 6 个基准（中证500/1000、国证2000、创业板指、科创50）alpha 全在 -1.9%~-3.6%，
   **换基准解决不了问题** → H1「基准错配」被证伪，不是主因。
6. 决定性一轮：回归 + 二项检验（见下）。

### 真正的两个口径缺陷（这才是根因）
**A. beta 敞口被算成选股能力。** `alpha = ret - 1.0×bench` 隐含 beta=1，
而 RPS 榜按定义筛的就是高波动小盘成长股。以入选日为观测单位 OLS：
**beta≈3.75（R²=0.56，相关 +0.746）**，截距 -2.79%/5日。
beta 校正后超额中位数从 **-3.61% 收敛到 -1.79%**；独立窗口口径 **-0.83%、胜率 50.0%**。
逐窗口看更清楚：20260817 原始 alpha -6.45%，beta 调整后 **+3.89%**（那周基准跌 3.75%）。
→ 高 beta 组合在跌市里的放大跌幅，1:1 减法会整笔记到「选股能力」头上。

**B. 样本量虚高一到两个数量级。** n=2210 里含 T+5 重叠窗口（相邻日共享 4 天持有期）
+ 同日 60 只截面高相关。折算不重叠窗口只有 **8 个**，3 胜 5 负，
**双侧精确二项检验 p=0.727**，无法拒绝「胜率=50%」；t=-0.90，95%CI [-13.7%, +6.1%] 跨 0。
日间波动/日内波动 = 0.87。
→ 「稳定反向超额」这个说法本身就不成立，样本还不够judge方向。

### 落地（`scorecard.py` 新增两函数 + 全站免责）
- `estimate_beta(rets, horizon)`：入选日为观测单位 OLS，输出 beta/R²/截距/
  校正后 alpha 中位数与胜率/独立窗口口径。`MIN_DAYS_BETA=15` 不足则拒绝给 beta。
- `effective_sample(rets, horizon)`：折算独立观测数 + 手写 `_binom_p_two_sided`
  （只允许 numpy/pandas，不引 scipy）。`MIN_INDEPENDENT=20`，不足时 reason 明写
  **「负数不等于策略失效，正数也不等于策略有效」**。
- `bucket_monotonic` 的 verdict 从「排序信息量存疑」改为「暂未观察到区分度」——
  分档样本同样受重叠影响，原措辞越界了。
- 展示层 `page_scorecard.py` 新增两个板块，**顺序刻意**：先「有效样本量」再
  「beta 拆解」再「区分度」，否则用户会先把噪音当结论。
- 合规：`app.py` 删「核心玩法：只买 RPS 大于 87 的股票」→ 中性描述 + 双重提示；
  免责放榜单**上方**（页脚等于没放）；侧边栏挂全站声明；战绩页页脚扩四条。
- `daily_scorecard.py` 跑批日志同时打 beta 与有效样本量——只打原始 alpha 会让
  日志本身成为误读源头。

### 自检（新增 `tools_probe_scorecard_ui.py`，AST 结构断言）
- 计算层探针 +148 行正反双向：构造 beta=2 须估出 2、构造 beta=1 须估出 1
  （**防恒定输出**）；全胜 p 须显著、对开 p 须不显著；二项检验算例对账
  (k=3,n=8)=0.7265625。
- 页面探针用 `ast.FunctionDef` 取**函数体源码切片**再断言，不做全文扫描——
  「免责声明写在别的函数里」和「写在这个函数里」全文匹配分不出来。
- 扫建议性措辞（目标价/只买/满仓…）必须**先剔除否定语境行**
  （免责声明本身就含「不提供目标价」），并给过滤器本身加正反自检。
  这个坑 `tools_probe_digest.py` 已踩过一次。
- 破坏式反向验证 3 组：beta 恒为 1.0 / 独立观测数=原始条数 / 删掉免责句 → 均如实 FAIL。
- 旧版 `performance.json`（无新字段）四种降级路径（空/None/failed/insufficient）不抛异常。
- `failed`（数据缺失，要修数据）与 `insufficient`（样本不够，只需等）**分开报**，
  混成一句会让人往错方向查。

### 终态
`tools_probe_scorecard.py`(39项) + `tools_probe_scorecard_ui.py`(28项) +
`tools_check_st_api.py` + run_daily/digest/sentiment 三探针全绿；
`performance.json` 用完即还原（**data/ 归云端**，本地实测后 `cp` 回原值再验 blob sha 相同）；
`_diag_tmp/` 已删；本地=远端 `8724570`，逐文件 API blob 比对 6/6 SAME，工作区干净。

## 任务三：定位并绕过 `CONNECT tunnel failed, response 502`（收尾）

记忆 commit `85852cf` 连续 3 次 push 全部 502，先走 API 兜底通道落地远端
（新 head `39e3f18`，两文件 blob sha 与本地 LF 归一化后逐一相同，tree 与本地
`85852cf^{tree}` 同为 `c0749062`），再回头定位根因。

### 根因
`HTTP_PROXY / HTTPS_PROXY / http_proxy / https_proxy` 全部指向 `http://127.0.0.1:63726`
——**本机代理端口已失效**。curl 走系统栈不吃这套环境变量，所以呈现「API curl 全绿、
git 全 502」的分裂现象，很容易误判成 GitHub 侧故障或网络抖动。

### 验证与解法
```bash
env -u HTTP_PROXY -u HTTPS_PROXY -u http_proxy -u https_proxy \
  git -c http.proxy= -c https.proxy= -c http.version=HTTP/1.1 fetch origin main
```
一次成功（`* [new branch] main -> origin/main`）；`ls-remote` 与 `push --dry-run`
同样通过。`-c` 与 `-u` 必须同时给：git 会同时读环境变量和自身 config。
502 是**确定性失败**，重试循环无意义。

顺带修掉一个自己写的坑：`if git push 2>&1 | tail -2; then break; fi` 的退出码取
`tail`（恒 0）→ 首次即 break，看着是重试三次实际只跑一次。

### 本机 ref 老问题复现
`fetch` 报成功但 `git rev-parse origin/main` 仍 `unknown revision`，
`.git/refs/remotes/origin/` 目录压根不存在。按 MEMORY 既有做法手写
`.git/refs/remotes/origin/main`，`for-each-ref` 复核通过后
`git reset --mixed 39e3f18` 对齐（tree 相同故工作区零变化）。
对齐后 `tools_probe_scorecard.py` + `tools_probe_scorecard_ui.py` 复跑全绿。

